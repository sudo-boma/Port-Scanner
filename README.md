# Port-Scanner
A lightweight Python port scanner that checks for open TCP ports (1-1024) on target hosts. Features hostname resolution, configurable timeouts, and detailed scan reporting. For educational use and authorized security testing only.
